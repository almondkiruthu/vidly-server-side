async (params: string) => {
  const string = "https://study-buddy-ai.vercel.app";
  let x = 10;
  if (x === 3) {
  } else {
  }
};

class Node {
  val: number;
  next: null;
  constructor(next: null, val: number) {
    this.val = val;
    this.next = next;
  }
}
